<?php

namespace CalcApiSig;

/**
 * Helper class to simplify generating a signature for APIs that use HMAC.
 *
 * @author forevermatt
 */
class HmacSigner
{
    public static function CalcApiSig($apiKey, $sharedSecret, $time = NULL)
    {
        // TEMP
        return '';
    }
}
